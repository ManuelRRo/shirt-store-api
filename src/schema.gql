# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Brand {
  id: String!
  name: String!
  products(offset: Int!, limit: Int!): [Products!]!
}

type Products {
  id: String!
  name: String!
  price: Float!
  brand: Brand!
  Variants: [Variants!]!
}

type Variants {
  id: String!
  size: Size!
  textColor: TextColors!
  rgb: String!
  stock: Int!
}

"""Sizes shirts"""
enum Size {
  NONE
  SMALL
  MEDIUM
  LARGE
  X_LARGE
  XX_LARGE
  S4YEARS_XS
}

"""Colors Shirts"""
enum TextColors {
  NONE
  GREEN
  BLUE
  YELLOW
  PINK
  SKY_BLUE
  BROWN
  BLACK
  WHITE
  ORANGE
  PURPLE
}

type Query {
  brands(offset: Int!, limit: Int!): [Brand!]!
  brand(id: String!): Brand!
  product(id: String!): Products!
  products(brand_id: String, parentCategory: String, categoryId: String, size: Size, textColor: TextColors): [Products!]!
}

type Mutation {
  newBrand(input: CreateBrandInput!): Brand!
  newProduct(input: CreateProductInput!): Products!
}

input CreateBrandInput {
  name: String!
}

input CreateProductInput {
  brand_id: String!
  name: String!
  price: Float!
  categoryId: String!
}